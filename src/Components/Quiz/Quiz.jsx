import Question from "./Question"

export default function Quiz({questionData}){

    // console.log(questionData)
    return (
        <div>
            {/* {
                questionData.map((question,i)=><Question key={i} sno={i} question={question}/>)
            } */}
        </div>
    )
}